# День 20 — данные одного слова в Java

- Дата начала: 23 сентября 2026 года.
- Статус: завершён 24 сентября с частичным результатом. Word и зависимости подготовлены, первая SQL-миграция не выполнена и перенесена на следующую встречу. Итог — в result.md; нижеследующие задания сохраняют содержание выданного урока.
- Этап: первый цикл сохранения и получения слова через API и PostgreSQL.
- Ближайшая цель: описать данные одного слова и перейти к классу `Word`; различить тип, имя поля и конкретное значение.
- Основной навык: `JAVA-OBJECT`. Повторение: `JAVA-VALUE` и `CODE-STRUCTURE`.
- Ожидаемый результат ближайшего шага: Маша объясняет описание одного слова и самостоятельно переносит разобранный приём на его поля. Сохранение в БД — результат дальнейших частей итерации, пока не выполнен.

## Исходная точка

В `D:\Projects\english-from-90s-back` есть основной класс, диагностический компонент с печатью фразы, JDBC-зависимости и настройки подключения. Классов слова, Spring Data JPA и миграций пока нет. Диагностический класс не расширяем. Приложение и базу при подготовке дня Codex не запускал.

## Вводная проверка

Маша верно различила тип, имя и значение в `String fabricName = "cotton";`. Для перевода выбрала String, указав, что других типов не знает. Уточнено: `String` пишется с большой буквы и подходит для текста. Другие типы пока не проверены; выбор типа с самостоятельным объяснением предстоит проверить.

## Объяснение и практика

Класс объясняется как описание устройства объектов, объект — конкретные данные по этому описанию, поле — переменная внутри класса для данных объекта. Аналогия: пустая карточка учёта ткани с графами названия и цвета и отдельные заполненные карточки. В Java это данные в памяти, не бумажные карточки и не строки базы автоматически.

Сторонний пример в чате — класс `Fabric` с полями `private String name;` и `private String color;`. Разобрать `public class`, имя класса, скобки, `private`, тип, имя поля и точку с запятой. Это пример для чтения; файл ткани в приложение не добавлять.

Практика с поддержкой: в серверной IDEA найти базовый пакет, создать подпакет `words` и класс `Word`. Подсказка по размещению даётся явно; самостоятельность выбора пакета этим не подтверждается.

Самостоятельный перенос: внутри класса объявить два текстовых поля `english` и `russian` по разобранному приёму. Готовый файл Word не выдавать. Объяснить, почему здесь выбран String. `id`, JPA, создание объектов и сохранение пока не добавлять без объяснения.

## Качество и проверка

- Название класса и файла — `Word` / `Word.java`; пакет `com.mashaikod.englishfrom90sback.words` соответствует пути.
- Поля описывают одно слово, имена английские, `camelCase`, тип написан в правильном регистре. Класс данных не содержит SQL, HTTP и вывода в консоль.
- Проверка ближайшего результата: чтение созданного файла и объяснение Маши. Этот класс пока не меняет интерфейс и не сохраняет запись; запуск не является доказательством работы сохранения.
- Перенос после проверки: объяснить, нужно ли менять описание класса для другого слова или для нового свойства, например примера употребления. Ответ не выдавать заранее.
- Точка остановки: два поля и понимание их назначения; затем по готовности объяснить объект и служебный идентификатор. Вся итерация API и БД не обязана уместиться в этот день.

## Следующий шаг — служебный id

После исправленной строки создания объекта перейти к идентификатору. Дополнительное понятие DATA-ID: устойчивый номер записи, отличный от текста слова и позиции строки. Новый тип Long разобрать на стороннем поле inventoryNumber; до выдачи номера такое поле может иметь null. Объявление не создаёт номер автоматически.

Маша самостоятельно переносит объявление на закрытое поле id типа Long внутри Word без присваивания числа. Проверка: правильные тип, регистр и имя, поле внутри класса; объяснение, должен ли меняться id при исправлении перевода. Создание записи и выдачу id базой пока не считать настроенными. Поле примера употребления из упражнения в первую запись не добавляется.

## Подготовка связи с таблицей

После проверки id объяснить соответствие: класс Word — описание записей таблицы words, сохранённый объект — строка, поля — столбцы. JPA задаёт правила такого соответствия, Hibernate реализует их, Spring Data JPA поможет с репозиторием. Термины углублять по месту использования.

Практика с поддержкой: в pom.xml заменить artifactId spring-boot-starter-jdbc на spring-boot-starter-data-jpa, затем выполнить Maven Reload в IDEA. Объяснить, что синхронизация скачивает библиотеки и обновляет сведения о проекте; она не сохраняет слово. PostgreSQL driver сохраняется. Проверка — чтение pom.xml и сообщение Маши о результате синхронизации или точный текст ошибки. После этого отдельно разобрать аннотации Entity и идентификатора; готовый файл сущности не выдавать.

## Entity и идентификатор

Зависимость Data JPA проверена в pom.xml; об отсутствии ошибок после синхронизации сообщила Маша. Следующая практика с поддержкой: после стороннего примера Fabric добавить в Word аннотации Entity над классом и Id над полем-идентификатором, импортируя обе из jakarta.persistence. До практики объяснить аннотацию и импорт, расположение относительно package и объявления класса. Полного готового Word не выдавать. Проверка — чтение файла, правильные импорты, прогноз места Id при другом имени поля. Аннотации не считать автоматическим сохранением; генерацию номера, таблицу и схему разобрать далее.

## Имя таблицы

После проверки Entity и Id разобрать @Table(name = "fabrics") на стороннем классе Fabric, импорт Table, параметр name и строковое значение. Самостоятельный перенос — указать для Word таблицу words. Проверить имя по соглашению и расположение над классом. Изменённое условие — таблица vocabulary: что требуется поменять в описании соответствия? Table само по себе не создаёт таблицу; генерация id и способ создания схемы остаются отдельными шагами.

## Генерация идентификатора

После исправления Table разобрать GeneratedValue со стратегией GenerationType.IDENTITY на поле inventoryNumber стороннего класса Fabric. Объяснить название параметра strategy, именованный вариант без кавычек, отличие от Id и необходимость identity-столбца в БД. Практика — два импорта GeneratedValue и GenerationType из jakarta.persistence и перенос аннотации на id. Проверка: правильное поле и импорты, прогноз new Word() против вставки в базу. Схему ещё не создавали; следующий шаг — выбор способа её создания, Liquibase пока вариант.

## Создание схемы через Liquibase

Для практики выбран ранее обсуждавшийся Liquibase с SQL. Объяснить миграцию как сохранённое изменение структуры и учёт её выполнения. Ближайший перенос знакомого приёма — добавить отдельный starter Liquibase в pom.xml рядом с другими зависимостями, без удаления JPA, и синхронизировать Maven. Координаты: org.springframework.boot:spring-boot-starter-liquibase. Это подготовка; запуск до настройки changelog не назначен. Следующая часть — сторонний SQL-пример, самостоятельная таблица words с identity-столбцом, путь changelog и проверка через запуск/IDEA. Реализацию пишет и запускает Маша. Создание таблицы и повторный запуск ещё не проверены.

## Первая SQL-миграция

Liquibase starter проверен, отсутствие видимых ошибок синхронизации — со слов Маши. До практики разобрать сторонний CREATE TABLE fabrics с id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY и name TEXT, объяснить каждую конструкцию и связь с типами Java. Две служебные строки formatted SQL — отдельный разбор; changeset id не является id записи.

Место самостоятельной работы: src/main/resources/db/changelog/db.changelog-master.sql в сервере. Один changeset masha:create-words создаёт words с тремя столбцами, соответствующими Word. Таблицу с готовым SQL за Машу не писать. Текстовые столбцы пока TEXT без дополнительных ограничений; правила ввода определить перед реализацией приёма слов. Проверить синтаксис, имена, соответствие identity стратегии и отсутствие лишней запятой. Прогноз — будут ли строки после создания таблицы. Путь changelog и запуск объяснить после проверки файла; пока это подготовленный текст, не выполненная миграция.
