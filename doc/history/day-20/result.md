# День 20

- Даты: начало — 23 сентября, завершение — 24 сентября 2026 года.
- Состояние занятия: завершён по просьбе Маши с частичным результатом.
- Выполнение задания: частично. Создана JPA-сущность Word с двумя текстовыми полями, id и аннотациями; добавлены Data JPA и Liquibase. SQL-задание не выполнено, миграция и сохранение перенесены на следующее занятие.
- Качество нового кода: текущий Word принят по чтению; Table исправлена, обязательное замечание закрыто. Запуск не выполнялся.
- Исходная точка: фронтенд `79c817d`, сервер `7911cf1`. Рабочая копия сервера чистая. Во фронтенде до занятия изменён результат дня 19 и имеются неотслеживаемые промпты и изображения обложек дня 19; они не относятся к новому уроку.
- Точка продолжения для дня 21: вернуться к ещё не выполненному SQL-заданию небольшими частями — CREATE TABLE и столбцы, затем formatted SQL changelog для words, путь в настройках и проверка применения. Перед запуском подготовить changelog: зависимость Liquibase уже добавлена. День 21 заранее не создавать.

## Итог при завершении

Маша прямо сообщила, что последнее SQL-задание не сделала, и предложила остановиться. День завершён без новых заданий или проверочных вопросов. Повторное чтение подтвердило корректный Word с Entity, Table(name = "words"), Id и GeneratedValue(IDENTITY), а также зависимости Data JPA и Liquibase. В resources только application.yaml, SQL-файла нет. Код этой части принят по чтению, все замечания к нему закрыты. Сборка, запуск и фактическое сохранение не проверялись.

Подтверждены отдельные ответы: различие типа/имени/значения, два объекта для двух new, единая структура двух слов, расположение Id независимо от имени поля, изменение параметра name при другом имени таблицы, выдача id при вставке. Практика шла со сторонними примерами и конкретными подсказками; воспроизведение синтаксиса ещё требует поддержки. Самостоятельное освоение тем не заявляется. SQL-попытки и ответа на прогноз нет; решение остановиться не является доказательством непонимания SQL.

Следующая встреча получает номер 21: возобновить создание таблицы с небольшого разбора SQL, не требовать сразу весь файл. Повторить JAVA-VALUE и CODE-NAME на столбцах и их типах; JPA-MAPPING и DATA-ID проверить на реальной строке после создания схемы. Подготовлен [текст для YouTube](youtube.md), полное содержимое показывается Маше в итоговом ответе. Публикация и Git-коммиты не выполнялись.
## Фактический результат подготовки

Маша попросила продолжить день 20. По файлам день 19 завершён частично, каталог дня 20 ещё отсутствовал. Начат день 20 с сохранённой предметной цели; отсутствующие предыдущие ответы не восстанавливались по предположению.

Прочитаны учебные правила, маршрут, реестр, задание и результат дня 19. Проверены перечень серверных файлов, `pom.xml`, основной класс, `DatabaseConnectionCheck.java` и настройки приложения без вывода пароля. Настройки JDBC присутствуют, компонент по-прежнему только печатает фразу. Классов слова и JPA-зависимости нет. Приложение, Docker, сборка и тесты не запускались; работа подключения сегодня не проверена.

## Понимание и помощь

Маша верно ответила: тип — string, имя — fabricName, значение — cotton. Для перевода предложила string и добавила: «я другие типы не знаю». Распознавание частей объявления подтверждено; объяснение выбора типа через назначение пока не получено. Уточнены написание `String` и использование для текста. Это сообщение не доказывает незнание всех типов в JavaScript и не требует отдельного обзора типов Java.

Следующая помощь: разобранный сторонний пример `Fabric` с двумя закрытыми текстовыми полями и аналогия карточки ткани; явное место создания `Word` в серверном пакете. Самостоятельная часть — объявить поля слова и объяснить тип. Факт новой попытки и полезность аналогии пока не проверены. Статусы навыков не повышены.

## Продолжение

День завершён; фактический итог и остаток указаны выше. Сохранение и чтение слова через API остаются целью итерации, для схемы выбран Liquibase с SQL. Повторение JavaScript вернуть при ближайшей работе с фронтендом, не заменять оценкой Java.

## Проверка 24 сентября 2026 года

Маша сообщила «готово» и объяснила выбор String тем, что он подходит для текста. Чтением подтверждён `src/main/java/com/mashaikod/englishfrom90sback/words/Word.java`: правильный пакет, `public class Word`, два поля `private String english;` и `private String russian;`. Обязательных замечаний к этому фрагменту нет. Сборка и запуск не выполнялись; класс пока только описывает данные, работа сохранения не проверена.

Степень помощи: сторонний пример Fabric с такими же типами полей, заранее предложенные имена и точный путь создания класса. Маша перенесла приём на Word и повторила верное обоснование типа после объяснения. Это полезный результат, но ещё не подтверждён перенос при изменённых условиях. Предложены вопросы о двух разных словах и новом свойстве «пример употребления», ответы пока не получены.

Git также показывает изменение Compose и application.yaml и удаление диагностического класса. Эти изменения не приписывались текущей практике с полями и не проверялись в рамках её приёмки; перед запуском сервера потребуется проверить актуальные настройки и ссылки. Новая календарная дата продолжает день 20, завершения занятия Маша не просила.
## Перенос: структура и имя поля, 24 сентября

Маша объяснила, что для cat и dog не нужны разные классы, поскольку структура одинакова, но добавила «можем их в один класс добавить». Верный вывод об общей структуре подтверждён; формулировка требует уточнения: один класс Word описывает устройство, а данные двух слов находятся в двух отдельных объектах. Создание объектов ещё не изучалось.

Для нового свойства предложила в чате `private String example of usage in English;`. Верно выбрала новое текстовое поле и String. Ошибка — пробелы внутри предлагаемого имени: Java разбирает их как отдельные слова, такое объявление не скомпилируется. Объясняется camelCase на стороннем имени fabricColor, затем предложено самостоятельно исправить строку в чате. Готовое имя поля не выдаётся. Реальный Word.java перечитан: в нём по-прежнему два корректных поля, ошибочная строка в файл не добавлена. Качество уже проверенного файла остаётся принятым; перенос с новым именем пока не завершён.

Ближайшее действие: получить исправленную строку в чате, проверить имя; затем продолжить объект и идентификатор. Новое поле пока только учебный пример, объём первой записи в БД не расширен. Статусы самостоятельности не повышены.



## Исправление имени и переход к объекту

Маша прислала `private String exampleOfUsageInEnglish;`: пробелы внутри имени убраны, camelCase применён корректно. Исправление выполнено после напоминания правила и стороннего примера fabricColor, без выдачи готового имени. Замечание к ответу закрыто; это одна попытка с поддержкой, статус CODE-NAME остаётся «Нужна практика». Добавление поля в приложение не запрашивалось и не проверялось.

Следующая порция: создание объекта на примере `Fabric firstFabric = new Fabric();`, разбор типа, переменной, присваивания, new и вызова конструктора без аргументов. Показать, что поля name и color ещё не заполнены и имеют null; отличить null от текстового значения. Самостоятельный перенос — написать в чате строку создания объекта Word с переменной firstWord и назвать количество объектов после двух отдельных new. В проект пример не вставлять: место исполняемых команд внутри метода будет разобрано отдельно. Ответа на новую практику пока нет.

## Первая строка создания Word

Маша ответила `Word firstWord = new Word()` и верно указала два объекта для двух отдельных new Fabric(). Перенос типа, имени переменной и выражения создания выполнен правильно по стороннему примеру. В ответе пропущена завершающая точка с запятой; указано конкретное место исправления, повторная строка пока не получена. Это ошибка записи в чате, не найденная ошибка файла приложения. Объяснение назначения ссылки своими словами ещё не получено; полного освоения JAVA-OBJECT не фиксируем.

Ближайший шаг: Маша дописывает точку с запятой в строке в чате; затем перейти к служебному id и объяснить новый числовой тип. Приложение не менялось и не запускалось.


## Исправление строки и идентификатор

Маша прислала `Word firstWord = new Word();` — строка корректна, замечание о точке с запятой закрыто после адресной подсказки. Word.java повторно прочитан: пока два текстовых поля, id отсутствует. Приложение не запускалось.

Следующая порция — DATA-ID в рамках описания объекта: идентификатор отличает запись от её текста и места в списке. Аналогия — учётный номер карточки ткани; номер не изменяется при исправлении её описания. Показан сторонний фрагмент `private Long inventoryNumber;`, разъяснены Long как тип для целого числа и возможность null до выдачи номера. Уточнено: само объявление поля не генерирует номер, выдача будет настроена при связи с БД.

Практика с поддержкой: Маша добавляет в Word закрытое поле id типа Long без начального значения; имена и тип предложены, готовый файл не выдаётся. Строку создания firstWord в класс вставлять не требуется. Проверка понимания — должен ли измениться id при исправлении перевода существующей записи и почему. Новая попытка и ответ пока не получены.


## Проверка поля идентификатора

Маша сообщила «готово» и ответила, что id не изменится, «так как вообще не меняется». Прогноз редактирования верен, но обоснование пока повторяет правило. Уточнено: при исправлении перевода сохраняется та же запись; удаление и создание новой записи — другая ситуация. Понимание устойчивой идентичности пока не подтверждено самостоятельным объяснением.

В Word.java фактически добавлено `private Long inventoryNumber;`. Тип Long, private, расположение и синтаксис верны. Имя перенесено из примера ткани вместо заданного id. Обязательное замечание: inventoryNumber означает инвентарный номер и вводит посторонний смысл в модель слова; по договорённости служебное поле называется id. Нужно переименовать существующее поле, не добавлять второе. Дана адресная подсказка, исправление пока не получено. Не объявлять это ошибкой компиляции или запретом Java на другое имя. Код и зависимости прочитаны, JPA по-прежнему не подключена; запуск не выполнялся.

Следующий шаг: повторно прочитать переименование inventoryNumber в id, затем перейти к связи класса с таблицей. Самостоятельность не повышена.


## Повторная проверка id и подготовка JPA

После «исправила» повторно прочитан Word.java: поле теперь `private Long id;`, прежнего inventoryNumber нет. Обязательное замечание закрыто по фактической правке. Три поля и пакет корректны. Переименование выполнено по адресной подсказке; самостоятельность не повышена. Сборка и запуск не выполнялись.

Проверен pom.xml: Spring Boot 4.1.1, JDBC starter и PostgreSQL driver присутствуют, Data JPA starter пока отсутствует. Официальная документация Spring Boot 4.1.1 проверена: https://docs.spring.io/spring-boot/reference/data/sql.html#data.sql.jpa-and-spring-data . Следующая небольшая практика — заменить artifactId существующего JDBC starter на spring-boot-starter-data-jpa и синхронизировать Maven в IDEA. JPA starter включает необходимые JDBC-компоненты; драйвер PostgreSQL остаётся нужен. Объясняется зависимость как подключаемая библиотека, сопоставление класса с таблицей, объекта со строкой и полей со столбцами. Само подключение библиотеки ещё не настраивает Entity или сохранение.

Синхронизация загрузит зависимости и обновит модель проекта IDEA; действие выполняет Маша. До её результата не считать библиотеку подключённой. Затем перейти к аннотациям Entity, Table и Id небольшими частями. Изменение схемы пока не назначено, Liquibase остаётся вариантом.


## Поиск панели Maven

Маша спросила, где панель Maven. Дана адресная инструкция для IDEA: View → Tool Windows → Maven; панель также может быть свёрнута в правой боковой полосе. Предложено навести указатель на кнопку обновления и найти подсказку Reload All Maven Projects либо Sync All Maven Projects. Если пункта Maven нет, сообщить об этом для проверки распознавания pom.xml. Открытие панели и успех синхронизации пока не подтверждены. Вопрос об интерфейсе не является доказательством непонимания зависимостей.

## Проверка зависимости и начало Entity

Маша сообщила: «готово, ошибок не вижу». Чтение pom.xml подтвердило замену JDBC starter на spring-boot-starter-data-jpa, драйвер PostgreSQL с runtime сохранён. Замечаний к изменению нет. Отсутствие видимых ошибок синхронизации — со слов Маши; Maven, сборка и сервер Codex не запускал. Word.java по-прежнему содержит english, russian и id без аннотаций.

Следующая порция — Entity и Id. Объясняется аннотация как метка для библиотеки: @Entity над классом обозначает сущность для хранения, @Id над полем — её идентификатор. Сторонний пример Fabric с Long inventoryNumber демонстрирует расположение; Маша переносит метки на Word и его id. Импорты jakarta.persistence.Entity и jakarta.persistence.Id объясняются как указание пакета нужных аннотаций, находятся после package и до класса. Само имя id аннотацию не заменяет. Автоматическая выдача номера, имя таблицы и создание схемы будут отдельными шагами; текущая порция не означает готового сохранения.

Официальная документация повторно проверена: https://docs.spring.io/spring-boot/reference/data/sql.html#data.sql.jpa-and-spring-data . Проверка переноса: куда поставить @Id при другом имени поля-идентификатора. Ответ и изменённый файл ещё не получены.


## Проверка Entity и Id

Маша верно ответила, что при имени идентификатора recordNumber аннотация Id ставится над recordNumber. Перенос расположения метки подтверждён ответом. Чтение Word.java подтвердило импорты jakarta.persistence.Entity и jakarta.persistence.Id, Entity над классом, Id над Long id. Обязательных замечаний нет. Изменение выполнено после стороннего примера и инструкции; самостоятельное объяснение всего соответствия ещё не проверено. Сервер и сборка не запускались, таблица и сохранение не проверены.

Следующая небольшая практика — Table с параметром name. На примере @Table(name = "fabrics") над Fabric объясняются аннотация, параметр name, строковое значение в кавычках и отличие имени Java-класса от таблицы. Маша добавляет импорт jakarta.persistence.Table и переносит метку на Word для согласованной таблицы words. Это описание соответствия, не команда создания таблицы. Проверочный прогноз: если таблица называлась бы vocabulary, нужно ли переименовать класс Word или изменить настройку Table? Ответ пока не получен. Генерация id и выбор способа создания таблицы следуют отдельными шагами. Документация проверена: https://jakarta.ee/specifications/persistence/3.2/apidocs/jakarta.persistence/jakarta/persistence/table .


## Проверка Table

Маша верно ответила: при другом имени таблицы меняется значение настройки name, а не имя класса. Чтение Word.java обнаружило импорт Table и строку `@Table(words)`. Обязательное замечание: пропущены имя параметра name и кавычки вокруг строкового значения. Для Table сокращённая форма без name не подходит, а words без кавычек не обозначает строковый литерал; такое объявление не скомпилируется. Сборка не запускалась, вывод сделан по коду.

Объяснить две части на прежнем стороннем примере @Table(name = "fabrics"): name — название настройки, "fabrics" — её текстовое значение. Маша самостоятельно исправляет свою аннотацию для words; готовый файл не выдаётся. Прогноз о смене значения верен, но применение синтаксиса пока требует поддержки. Переход к генерации id отложен до проверки исправления. Остальные импорты, Entity, Id и поля сохранены корректно.


## Table исправлена; генерация id

После «исправила» повторно прочитан Word.java: @Table(name = "words") и импорт Table корректны, замечание закрыто. Остальные поля и аннотации сохранены. Правка выполнена после адресной подсказки о name и кавычках; статус самостоятельности не повышен. Сборка и запуск не выполнялись.

Следующий шаг — @GeneratedValue(strategy = GenerationType.IDENTITY) над полем идентификатора. Объяснить отличие Id (какое поле является ключом) от GeneratedValue (как получить значение). На стороннем поле Fabric.inventoryNumber показать обе аннотации. strategy — параметр; GenerationType.IDENTITY — именованный вариант стратегии, не строка, кавычки не нужны. При IDENTITY номер выдаёт настроенный identity-столбец базы при вставке строки; одной аннотации без настройки таблицы недостаточно. До сохранения после new у Long-поля null. Это не счётчик в Java.

Маша добавляет импорты jakarta.persistence.GeneratedValue и jakarta.persistence.GenerationType, затем переносит аннотацию на своё поле id рядом с Id. Полный Word не выдаётся. Проверка понимания: появляется ли номер уже после new Word() или при вставке в базу, когда таблица настроена. Ответ и изменение пока не получены. Затем выбрать способ создания схемы; Liquibase остаётся предложенным вариантом, не уже выбранным инструментом. Официальные определения проверены ранее в этом диалоге: https://jakarta.ee/specifications/persistence/3.2/apidocs/jakarta.persistence/jakarta/persistence/generatedvalue и https://jakarta.ee/specifications/persistence/3.2/apidocs/jakarta.persistence/jakarta/persistence/generationtype .


## GeneratedValue проверена; подготовка таблицы

Маша сообщила о выполнении и верно ответила, что IDENTITY выдаёт номер при добавлении записи в настроенную таблицу. Word.java прочитан: GeneratedValue со стратегией GenerationType.IDENTITY стоит над id рядом с Id, оба импорта корректны. Обязательных замечаний нет. Ответ получен после разбора механизма; работоспособность вставки и самостоятельное объяснение не проверены. Сборка, сервер и база не запускались.

Проверены текущие настройки без вывода секретов: JDBC URL использует localhost:15432, Compose — 127.0.0.1:15432:5432, база и пользователь совпадают. Это проверка файлов, не соединения. В resources пока только application.yaml, зависимости Liquibase и файлов миграций нет.

Для следующего учебного шага преподаватель выбирает ранее обсуждавшийся Liquibase с SQL: нужен явный скрипт создания таблицы и учёт выполненного изменения. Это выбор способа в рамках занятия, не новое пожелание Маши. Объяснение: миграция — сохранённое изменение структуры БД, например создание таблицы; Liquibase применяет ещё не выполненные изменения и хранит их историю. SQL будет разобран на стороннем примере отдельно.

Ближайшее действие Маши — добавить отдельную зависимость org.springframework.boot:spring-boot-starter-liquibase в существующий dependencies по образцу соседнего starter, без своей версии, и синхронизировать Maven. Data JPA остаётся, так как назначение другое. До создания changelog запуск приложения не назначается: конфигурация Liquibase ещё неполная. Проверить XML и результат синхронизации, затем перейти к первой SQL-миграции и настройке её пути. Схемой управляет Liquibase; автоматическое изменение схемы Hibernate параллельно не включать. Официальная документация Spring Boot 4.1.1 проверена: https://docs.spring.io/spring-boot/how-to/data-initialization.html .


## Liquibase подключён; первая SQL-миграция

После сообщения «все готово и не вижу ошибок» прочитан pom.xml: отдельная зависимость org.springframework.boot:spring-boot-starter-liquibase добавлена правильно, JPA и драйвер сохранены. Видимых ошибок синхронизации нет со слов Маши; Codex Maven не запускал. Word с аннотациями не изменился. В resources пока только application.yaml, миграций ещё нет.

Следующая порция — SQL CREATE TABLE и formatted SQL changelog. Сторонний пример для fabrics: id типа BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY и name типа TEXT. Разобрать CREATE TABLE, круглые скобки, отдельные объявления столбцов, запятую, точку с запятой, BIGINT в соответствии с Long, TEXT для текста, автоматическую выдачу числа и PRIMARY KEY (уникальность и отсутствие NULL). Типы Java в SQL не копируются. Ограничения текстового ввода в этой порции не вводятся; их определить перед приёмом данных.

Две служебные строки Liquibase объяснить отдельно: --liquibase formatted sql в начале файла и --changeset author:id перед изменением. Id changeset обозначает изменение структуры, не id слова. Пример автора demo и изменения create-fabrics; в самостоятельном переносе автор masha и имя изменения create-words.

Практика: Маша создаёт src/main/resources/db/changelog/db.changelog-master.sql через каталоги db и changelog и New File, затем пишет один changeset для таблицы words со столбцами id, english и russian по примеру ткани. Имена должны соответствовать Word; полного SQL для words не выдавать. Для строковых столбцов TEXT, для id — разобранный identity и ключ. Файл пока не подключается к запуску: после чтения миграции отдельно объяснить spring.liquibase.change-log и проверку схемы. Проверка понимания: содержит ли таблица слова сразу после одного CREATE TABLE? Ответа пока нет. Запуск и создание таблицы не выполнялись.

Источники: https://www.postgresql.org/docs/17/ddl-identity-columns.html , https://www.postgresql.org/docs/17/datatype-character.html , https://docs.liquibase.com/community/user-guide-5-0-4/sql-changelog-example .
