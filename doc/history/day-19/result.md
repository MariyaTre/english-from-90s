# День 19

- Даты начала и завершения: 22 сентября 2026 года.
- Состояние занятия: завершён по просьбе Маши, с частичным результатом.
- Выполнение задания: частично; зависимости и настройки добавлены, запуск Java подтверждён присланным выводом, SQL-запрос ещё не выполнен.
- Качество кода: зависимости, настройки и диагностический класс приняты по чтению, замечание к размещению закрыто. Работа сохранения в БД ещё не реализована и не проверена.
- Исходная точка: фронтенд `8f7afda`, сервер `82d33c3`. До занятия во фронтенде уже изменены `doc/history/day-18/result.md`, `youtube.md`, добавлены `thumbnail-prompt.md` и `youtube-thumbnail.png`; они не относятся к дню 19. Рабочая копия сервера чистая.
- Точка продолжения для дня 20: по пожеланию Маши перейти к сохранению и получению слова через API. Начать с полей английского слова, перевода и служебного `id`, затем Entity `Word` в подпакете `words`. Запросы имени базы и пользователя сняты с задания; диагностический класс не расширять. Liquibase с SQL пока вариант. Следующее занятие заранее не создано.

## Фактический результат

По просьбе Маши из обложки v2 удалена доска «Ошибки дня». Актуальная обложка — [youtube-thumbnail-v3.png](youtube-thumbnail-v3.png), [промпт imagegen](thumbnail-prompt-v3.md). Удаление доски и сохранение остальных надписей проверены визуально; предыдущие версии сохранены.

По следующему запросу Маши обложка переделана на предоставленной основе дня 6 с двумя досками. Актуальный вариант — [youtube-thumbnail-v2.png](youtube-thumbnail-v2.png), [промпт](thumbnail-prompt-v2.md). Проверены надписи дня 19 и три трудности: Docker не запущен, лишние скобки, класс не в том пакете. Использован встроенный imagegen; исходник и первый вариант сохранены, публикация не выполнялась.

После завершения дня по отдельному запросу Маши создана [обложка YouTube дня 19](youtube-thumbnail.png) на основе предоставленной обложки дня 17. Использован встроенный imagegen, исходник не изменялся. Визуально проверены номер дня, текст о первой базе в Docker и подписи PostgreSQL / Java + Spring / :5432; композиция и персонаж сохранены. [Промпт и сведения о генерации](thumbnail-prompt.md) сохранены. Публикация не выполнялась; учебный день не возобновлялся.

Итог при завершении: PostgreSQL подготовлена с мужем; её готовность проверена Codex в ходе дня. JDBC-зависимости и настройки добавлены Машей по примерам и исправлены после подсказок. Запуск Java подтверждён присланным логом. Создан и правильно размещён компонент `DatabaseConnectionCheck` с методом `run`, печатающим «Маша и код»; код повторно прочитан при завершении, вывод этого метода не получен. Реальное обращение Java к базе и сохранение слов не выполнены. Предметных Entity, репозитория, сервиса, контроллера и миграций нет. Текущие обязательные замечания к прочитанным изменениям закрыты, но функциональная цель дня целиком не достигнута.

Маша попросила закончить сегодня и продолжить завтра. Новых заданий и проверочных вопросов на завершение не выдавалось. Подготовлен [текст для YouTube](youtube.md); полное содержимое показывается в итоговом ответе. Публикация и Git-коммиты не выполнялись. Оценки самостоятельности не повышены: большая часть действий выполнялась с подробной поддержкой.

Следующая проверка навыков: `JAVA-VALUE` и `JAVA-OBJECT` — поля одной записи слова; `CODE-STRUCTURE` — место предметного класса; `JAVA-METHOD` — назначение действия сохранения/чтения. `JS-VAR` и `JS-TYPE` вернуть при ближайшем JavaScript-шаге (ориентир дни 20–21), не подменять их оценкой Java. Лишние диагностические упражнения к предметной функции не добавлять. Основания отдельных наблюдений далее сохраняют ход дня и не заменяют этот итог.

Маша передала мнение мужа: вместо запросов имени базы и пользователя полезнее создать контроллер, сервис с сохранением/получением, репозиторий и Entity, выполнить запросы и увидеть данные в таблице через IDEA; Liquibase с SQL предложен как возможный вариант. Принято направление на полезную предметную функцию, текущий урок и маршрут обновлены. Объяснено: диагностический запрос мог проверить реальное подключение, но на нём неоправданно задержались. Наличие настроек не является доказательством соединения; теперь это проверит само сохранение слова. Степень помощи и результаты предыдущих действий сохраняются. Перечень файлов подтверждает отсутствие предметных классов; существующий `DatabaseConnectionCheck` всё ещё только печатает «Маша и код». Его выполнение после последней правки не показано, удаление или изменение Codex не выполнялись. Учебный день продолжается с новой целью, создание миграций не объявлено выполненным или обязательным.

Получена строка `Started EnglishFrom90sBackApplication in 1.058 seconds (process running for 1.461)`: успешный старт подтверждён выводом Маши. Текущий основной класс прочитан, дополнительных Java-классов пока нет. Запрос к БД не выполнен. Разбирается фрагмент на других данных: объявление `String fabricName`, вызов `jdbcTemplate.queryForObject` с SQL `select 'cotton'` и типом результата `String.class`, затем вывод через `System.out.println`. Это пример для чтения, не готовая реализация и не инструкция вставить его в основной класс: JDBC-объект пока не передан диагностическому классу. Объясняются тип, имя, сохранение результата, SQL и метод. Предложены прогноз замены текста на `linen` и прогноз при остановке PostgreSQL; ответов пока нет. Официальная документация Spring JDBC и CommandLineRunner проверена.

Маша прислала две строки вывода запуска: `Starting EnglishFrom90sBackApplication using Java 25.0.4.1` из серверного `target/classes` и `Tomcat initialized with port 8080 (http)`. По этому выводу подтверждены начало запуска на указанной Java и настройка HTTP-порта; завершение старта, фактическая доступность HTTP и подключение к PostgreSQL ещё не подтверждены. Попросить заключительные строки, объяснив различие `Starting` и `Started`. Самостоятельное объяснение механизма пока не получено; запуск выполнялся по инструкции.

Маша попросила продолжить день 19. Прочитаны учебные правила, маршрут, реестр, история дня 18 и шаблоны. Проверены исходники существующего серверного каркаса и Git-состояние. Приложение, сборка и тесты не запускались; доступность адреса в этом занятии ещё не проверялась.

| Проверка | Фактический результат | Основание |
|---|---|---|
| Каркас сервера | Основной класс вызывает `SpringApplication.run`; в конфигурации только имя приложения, собственных контроллеров нет | Чтение файлов и перечня исходников |
| Запуск, остановка и запрос | Не проверено в день 19 | Запуск ещё не выполнялся |
| Понимание процесса и адреса | Маша назвала `8080` портом, «где открывается проект»; смысл `localhost` и прогноз остановки пока не проверены | Ответ Маши |
| Подготовка сервера | С установкой помог муж; степень участия в остальных действиях неизвестна | Со слов Маши в день 19 |

## Проверка качества и исправления

После «готово» проверен `DatabaseConnectionCheck.java`: оба импорта на месте, класс реализует `CommandLineRunner`, метод `public void run(String... args)` с `@Override` расположен внутри класса, скобки корректны. Маша выбрала фразу «Маша и код» для `System.out.println`. Обязательных замечаний к текущему фрагменту нет. Это выполнение по подробным указаниям с собственной подписью, не самостоятельное освоение метода. Запуск нового кода ещё не проверен; предложено остановить старый запуск основного класса и запустить снова, найти точную фразу в консоли. SQL и соединение с БД пока отсутствуют.

После сообщения «добавила 2 строки» прочитан диагностический класс. Импорт `org.springframework.stereotype.Component` расположен после `package`, `@Component` — перед объявлением класса; тело пока пустое. Обе строки добавлены корректно по данному примеру, обязательных замечаний нет. Запуск после этой правки не проверялся; выполнение по образцу не подтверждает понимание. Следующий шаг уменьшен до наблюдаемого вывода собственной фразы из `run`: объясняются метод, `void`, `String... args`, договорённость `CommandLineRunner`, `@Override` и вызов со стороны Spring. SQL и передача JDBC-объекта остаются следующими частями. Маша сама выбирает сообщение; это практика с поддержкой, а не полноценный самостоятельный перенос.

После сообщения «все» в контексте переноса выполнено чтение списка исходников и класса. Подтверждён путь `src/main/java/com/mashaikod/englishfrom90sback/diagnostics/DatabaseConnectionCheck.java` и соответствующее объявление `package com.mashaikod.englishfrom90sback.diagnostics;`; прежнего класса в корневом `diagnostics` в списке нет. Замечание к размещению закрыто. Перенос выполнен Машей с подробными подсказками по окнам IDEA; самостоятельное объяснение ещё не получено, статус навыка не повышен. Класс пока пустой. Сообщение относится к завершению переноса, не к завершению учебного дня. Следующий шаг — возврат к ранее разобранному каркасу и самостоятельному телу `run`.

Маша сначала спросила, где находятся пакеты: объяснены дерево `src/main/java` и отображение составного имени в IDEA. Затем прислала скриншот диалога Add File to Git после создания класса. Скриншот и чтение подтвердили `src/main/java/diagnostics/DatabaseConnectionCheck.java`, объявление `package diagnostics;`, пустое тело класса. Пакет находится вне базового пакета приложения. Обязательное исправление: перенести класс в `com.mashaikod.englishfrom90sback.diagnostics`, иначе будущий `@Component` не будет обнаружен стандартным сканированием приложения. Предложено закрыть Git-диалог кнопкой Cancel (файл сохраняется, добавление в Git откладывается), затем Refactor → Move для класса с полным целевым пакетом. Git, файлы приложения и перемещение Codex не выполняет. Исправление ещё не проверено.

После «исправила» выполнено повторное чтение: URL `jdbc:postgresql://localhost:5432/sewing_workshop`, пользователь `user`, структура YAML верна. Угловых скобок вокруг пароля больше нет, текст пароля совпадает со значением в Compose (выведены только логические результаты сравнения). Это не проверяет фактический пароль уже созданной БД. Замечание к настройкам закрыто по чтению; исправление выполнено Машей после конкретной подсказки. Наличие только основного Java-класса подтверждено перечнем файлов; SQL-запрос ещё не реализован. Следующий шаг — запуск Машей в IDEA с чтением результата; Codex приложение не запускал.

После следующего «готово» прочитан `application.yaml`: раздел `datasource` расположен верно рядом с `application`, отступы корректны; выбраны правильные порт, база и пользователь. Однако остались угловые скобки: `localhost:<5432>/<sewing_workshop>`, `<user>`, также скобки вокруг пароля (проверено как логический признак, без вывода секрета). Обязательное исправление: убрать маркеры шаблона. Последствие — некорректный порт в JDBC URL и отличающиеся от настроенных учётные данные. Дана адресная подсказка на другом имени базы: `<fabric_store>` заменяется целиком на `fabric_store`; обычные кавычки вокруг пароля можно сохранить. Исправление пока не получено; приложение не запускалось. Ответ о различии 5432 и 8080 пока отсутствует.

После сообщения «вроде как я сделала» прочитаны `pom.xml`, его diff и `application.yaml`. В существующий блок `dependencies` корректно добавлены `org.springframework.boot:spring-boot-starter-jdbc` и `org.postgresql:postgresql` с `runtime`. Вложенность и форматирование соответствуют файлу; обязательных замечаний к этой правке нет. Это выполнение по предоставленному фрагменту, не подтверждение самостоятельного выбора зависимостей. Maven, сборка и приложение Codex не запускались; успех синхронизации IDEA пока неизвестен. В YAML только имя приложения; следующий шаг — добавить `datasource` рядом с `application`, самостоятельно составить URL и подставить свои учётные данные.

При последующей проверке появился `_local/docker/docker-compose.yaml` (Git `AM`): PostgreSQL 17, локальное отображение порта 5432 и именованный том по корректному для версии пути. Файл расположен по действующему соглашению. База сохранила имя учебного примера `sewing_workshop`; это не препятствует подключению и не повод переименовывать существующую базу в ходе этого шага. Смысл имён уточнить при переходе к данным приложения. Java-код и настройки пока не изменены; при их появлении проверить ответственность и имена.

## Понимание и самостоятельность

На прогноз замены `cotton` на `linen` Маша ответила «linen» — верно; на вопрос о возможности получить ответ при остановленной PostgreSQL — «думаю нет», также верно. После этого объяснено, что SQL выполняет база, а Java получает результат. Это два верных прогноза после разбора, но ещё не самостоятельное написание и объяснение механизма; статусы до самостоятельности не повышены. Следующая порция — каркас отдельного `DatabaseConnectionCheck` с пустым `run`, объяснение класса, поля, конструктора, `this`, `@Component`, `CommandLineRunner` и `@Override`. Полного решения запроса нет: Маша сама переносит пример на SQL `select current_database()`, выбирает имя переменной и печатает результат. Перед запуском предложено предсказать имя базы. Код каркаса дан как поддержка в чате, реализацию Codex не создаёт. Ответ о портах 5432 и 8080 по-прежнему не получен.

Маша сообщила, что с установкой сервера помог муж. Это уточняет степень помощи при подготовке, но не доказывает затруднение с самостоятельным запуском. Она верно назвала `8080` портом; формулировка «где открывается проект» требует уточнения различия адреса и работающей программы. Объясняется: `localhost` указывает на свой компьютер, порт — номер точки подключения, запрос принимает работающий сервер. Оценки `SERVER-RUN`, `TOOL-RUN` и `HTTP-PATH` не повышены; нужен прогноз и практическая проверка.

## Вопросы и затруднения

Маша прямо сказала, что не понимает, куда записывать «разобранный каркас», что это и нужно ли другое действие. Повторное чтение подтвердило исходный пустой класс с правильным пакетом. Предыдущая порция оказалась слишком большой и не дала понятного места действия. Объясняется, что каркас — обычное слово «заготовка кода», а не команда или отдельный инструмент. Показаны назначение строки `package`, объявления класса и его фигурных скобок; место работы — редактор существующего файла в IDEA. Практика уменьшена до импорта `org.springframework.stereotype.Component` и аннотации `@Component` над классом с объяснением обеих строк. Предыдущее задание заполнить весь каркас и SQL пока приостановлено. Успешной новой попытки нет; из вопроса не делается вывод о незнании SQL или всех понятий Java.

Маша показала окно Move и уточнила, где указывать пакет. По скриншоту выбран режим `To package`, в верхнем поле стоит `diagnostics`, нижний путь ведёт в `src/main/java/diagnostics`. Дана адресная подсказка: заменить верхнее поле полным именем `com.mashaikod.englishfrom90sback.diagnostics`, проверить нижний целевой путь и нажать Refactor. Если путь не обновится, перенос пока не подтверждать, показать изменённое окно. Факт выполненного переноса ещё не получен; степень помощи — указание конкретного поля интерфейса.

После предложенного запуска Docker Desktop Маша ответила «работает». Проверено: `docker compose ... ps` показывает `docker-postgres-1`, образ `postgres:17`, состояние `Up` и порт `127.0.0.1:5432->5432/tcp`. Проверка `docker compose ... exec -T postgres pg_isready -U user -d sewing_workshop` вернула `accepting connections`, код 0. Это проверка готовности PostgreSQL внутри контейнера, не проверка пароля или подключения Java. Недоступность Engine на текущий момент устранена; самостоятельный механизм запуска Маша пока не объясняла. Чтение файлов подтвердило отсутствие JDBC-зависимостей и `spring.datasource`. Ближайшая порция инструкции — две зависимости и синхронизация Maven; изменений реализации Codex не вносил.

Маша сообщила: «у меня не подключаеться докер». Проверено без изменений окружения: `docker.exe` найден, процессы `Docker Desktop` и `com.docker.backend` не обнаружены; `docker info --format '{{.ServerVersion}}'` завершился с кодом 1: недоступен `npipe:////./pipe/dockerDesktopLinuxEngine`, система не находит указанный файл. Это подтверждает недоступность локального Docker Engine на момент проверки, а не ошибку пароля PostgreSQL. Вероятная причина — Docker Desktop не запущен. Ближайшее действие: Маша открывает Docker Desktop через меню Пуск, ждёт завершения запуска и сообщает результат или текст ошибки. Docker и контейнеры Codex не запускал и не менял. Самостоятельность не оценивалась по этому сообщению. В `application.yaml` по-прежнему только имя приложения.

На частичный ответ дано небольшое объяснение адреса через мастерскую: компьютер — здание, порт — номер приёмного окошка; программа должна работать, чтобы отвечать. Граница аналогии: порт — число, а не физическое место или папка проекта. После объяснения предложен прогноз остановки; нового ответа пока нет.

## Ход подготовки до смены цели

Маша сообщила о совместном с мужем создании БД, запуске контейнера и успешном подключении через IDEA и попросила инструкцию для связи Java с БД и первого действия. Compose подтверждён чтением; запуск и подключение подтверждены только её сообщением. В `pom.xml` пока отсутствуют JDBC starter и PostgreSQL driver; в `application.yaml` только имя приложения. Самостоятельность Docker не повышена. Объясняется различие клиента IDEA и Java-приложения; дан первый шаг подключения с примером Maven-зависимостей и конфигурации сторонней базы. Цель текущей практики — прочитать имя базы из Java, затем самостоятельно получить пользователя. Запросы и запуск Codex не выполнял, учётные данные в документацию не сохранял. Подключение Java пока не подтверждено. Официальная документация Spring Boot 4.1.1 и pgJDBC проверена.

Маша попросила тело Docker Compose для PostgreSQL. Поиск в серверном репозитории не обнаружил Compose-файла. Предоставляется разобранный пример для сторонней базы мастерской (`sewing_workshop`), с образом PostgreSQL 17, локальным портом и именованным томом. Файл приложения Codex не создаёт; самостоятельный перенос — выбрать имя своей базы и пользователя, локальный учебный пароль, объяснить изменение внешнего порта. Docker и база не запускались, наличие Docker не проверялось. Официальная документация образа и Compose проверена: https://hub.docker.com/_/postgres и https://docs.docker.com/reference/compose-file/services/#ports. Запрос сам по себе не подтверждает ни знание, ни отсутствие знания Docker. Степень помощи — полный сторонний пример с разбором; самостоятельное объяснение пока не получено.

Получить прогноз после объяснения адреса и перейти к небольшому действию в существующем серверном проекте; не требовать повторять всю диагностику. Повторение `JS-VAR` и `JS-TYPE` остаётся на ближайшую практику с данными JavaScript в дни 19–21; не добавлять отдельное упражнение к диагностике запуска без необходимости. Время и самочувствие пока не сообщены.
